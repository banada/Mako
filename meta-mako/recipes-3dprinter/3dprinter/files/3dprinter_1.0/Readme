    Web server and slice program of 3dprinter, we can use
wlan to acess 3dprinter and operate it to print 3D module
without install anything in your mobile device or computer.

step:

1.run the script:
  $ 3dprinter
2.connect to an ap .
3.your computer also connect to the same ap.
4.Run Firefox or other browsers,and input your board ip,
  you can use ifconfig to see wlan0's ip.


#You can use below comman to debug:

sudo /etc/init.d/nginx restart   //restart nginx

python flask.py //use browser acess 127.0.0.1:5000 //verify  flask

sudo /usr/local/bin/uwsgi -x app_config.xml -d /var/log/uwsgi.log   //start uwsgi
sudo tail -f -n 100 /var/log/uwsgi.log // can see error log
